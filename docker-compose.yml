services:
  api:
    build:
      context: .
      dockerfile: src/Dockerfile
    ports:
      - "8000:8000"

#  nginx:
#    image: nginx:1.25
#    ports:
#      - "80:80"
#    volumes:
#      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
#      - ./src/medias:/var/www/media:ro
#
#    depends_on:
#      - app